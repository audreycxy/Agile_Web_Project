// Run the code after the page has finished loading
document.addEventListener("DOMContentLoaded", () => {
    // Confirm that the Game History page script has loaded
    console.log("Game History page loaded.");
});